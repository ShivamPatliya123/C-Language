#include<stdio.h>
int main()
{
    int a[6],i,mid;
    int *p=&a[0],*p1=&i,*p2=&mid;
    for(*p1=0;*p1<6;(*p1)++)
    {
      printf("Element of array a[%d]:",*p1);
        scanf("%d",(p+i));
    }
    *p2=5/2;
    printf("middle value is=%d",a[*p2]);
    return 0;
}
