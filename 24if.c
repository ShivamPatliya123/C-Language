#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the value of a");
    scanf("\n %d",&a);
      printf("enter the value of b");
    scanf("\n %d",&b);
    if(a>b)
    printf("a is the max=%d");
    else
    printf("b is the max and a is small=%d");
    return 0;
}
